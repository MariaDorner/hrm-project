import logout from "./logout.svg";
const LogoutIcon = function () {
  return (
    <img
      src={logout}
      style={{
        width: "30px",
        height: "30px",
      }}
    />
  );
};
export default LogoutIcon;
